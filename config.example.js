// Copy this file to `config.js` and fill with your own Google API credentials.
// For production on Vercel, use environment variables and the provided build helper instead.
window.TASKFLOW_CONFIG = {
  GCAL_CLIENT_ID: "YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com",
  GCAL_API_KEY: "YOUR_GOOGLE_API_KEY",
  GCAL_SCOPES: "https://www.googleapis.com/auth/calendar"
};
